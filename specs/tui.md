# Planck TUI

> **Removed.** The TUI was planned but never implemented. The Web UI
> (`planck_cli/lib/planck/web/`) covers the interactive interface, and the HTTP
> API (`docs/guides/api.md`) covers external/headless access. There is no plan
> to add a TUI.
